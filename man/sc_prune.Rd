% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prune.R
\name{sc_prune}
\alias{sc_prune}
\title{Prune Cache Entries}
\usage{
sc_prune(
  cache,
  older_than = NULL,
  max_entries = NULL,
  max_bytes = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{cache}{A \code{semcache} object from \code{\link[=sc_open]{sc_open()}}.}

\item{older_than}{A \code{POSIXct} time; entries created before this are pruned.}

\item{max_entries}{Maximum number of entries to keep (oldest removed first).}

\item{max_bytes}{Maximum total blob size in bytes (oldest removed first).}

\item{dry_run}{If \code{TRUE}, return what would be deleted without deleting.}
}
\value{
A list with \code{n_removed} and \code{ids_removed} (invisibly).
}
\description{
Removes expired entries and optionally prunes to size/count limits.
}
\examples{
\donttest{
cache <- sc_open(tempfile("semcache_"), semantic = "off")
sc_put(cache, "q1", "a1")
sc_put(cache, "q2", "a2")
sc_prune(cache, max_entries = 1)
}
}
