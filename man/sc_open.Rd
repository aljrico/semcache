% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{sc_open}
\alias{sc_open}
\title{Open or Create a Semantic Cache}
\usage{
sc_open(
  path,
  semantic = c("auto", "off", "on"),
  storage = c("auto", "rds", "toon"),
  dim = NULL,
  metric = c("cosine", "l2"),
  version = "v1",
  strict = TRUE
)
}
\arguments{
\item{path}{Directory for the cache. Created if it does not exist.}

\item{semantic}{One of \code{"auto"}, \code{"off"}, or \code{"on"}.
\itemize{
\item \code{"auto"}: enable semantic mode if \strong{usearchlite} is installed and
dimensionality can be determined.
\item \code{"off"}: exact-only mode.
\item \code{"on"}: require \strong{usearchlite}; error if not available.
}}

\item{storage}{One of \code{"auto"}, \code{"rds"}, or \code{"toon"}.
\itemize{
\item \code{"auto"}: use \strong{toonlite} if installed, else RDS.
\item \code{"rds"}: store each entry as an RDS file.
\item \code{"toon"}: store each entry as a TOON file (requires \strong{toonlite}).
}}

\item{dim}{Integer embedding dimension (required for new semantic caches;
auto-discovered from existing config).}

\item{metric}{Distance metric: \code{"cosine"} (default) or \code{"l2"}.}

\item{version}{Cache version string (default \code{"v1"}).}

\item{strict}{Logical. If \code{TRUE} (default), semantic reuse requires matching
model, tool_schema_hash, version, and temperature bucket.}
}
\value{
A \code{semcache} object (environment-based S3 class).
}
\description{
Opens an existing cache or creates a new one at the given path. The cache
supports exact (hash-based) lookups and optional semantic (embedding-based)
lookups via \strong{usearchlite}.
}
\examples{
\donttest{
cache <- sc_open(tempfile("semcache_"))
print(cache)
}
}
